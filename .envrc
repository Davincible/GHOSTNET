# Solidity + SvelteKit Monorepo direnv configuration
#
# This file automatically loads the Nix shell when entering the directory.
# Run `direnv allow` to enable.

use nix

# Watch for dependency changes
watch_file shell.nix
watch_file apps/web/package.json
watch_file apps/web/bun.lockb
watch_file packages/contracts/foundry.toml
watch_file packages/contracts/remappings.txt

# Load .env if present (for contract deployment keys)
dotenv_if_exists
